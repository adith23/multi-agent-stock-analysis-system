# FinBERT artifacts

Model weights are deployment artifacts and are not committed to source control.
Pre-download the approved `ProsusAI/finbert` revision into this directory or the
deployment model cache.
