# Public, non-secret defaults used only when deployment build arguments are absent.
# Cloud Build overrides both values with the deployed backend URL.
NEXT_PUBLIC_API_BASE_URL=https://backend.example.invalid/api/v1
NEXT_PUBLIC_SSE_BASE_URL=https://backend.example.invalid/api/v1
